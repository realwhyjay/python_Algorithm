# 백준 1715 : 카드 정렬하기

#### Date : 21/03/29

#### Lang : Python

#### Question : [백준 1715](https://www.acmicpc.net/problem/1715)

## 1차 답안

```javascript
import heapq
n = int(input())
card = []
result = 0
for _ in range(n):
    heapq.heappush(card, int(input()))

while(len(card) != 1):
    x = heapq.heappop(card)
    y = heapq.heappop(card)
    heapq.heappush(card, x+y)
    result += (x+y)
print(result)

```



## 최종 답안 

1차 답안의 시간이 너무 오래 걸려서 for문 안에서는 input이 아닌 readline을 사용해주었다. 

```javascript
import heapq
import sys

n = int(input())
card = []
result = 0
for _ in range(n):
    heapq.heappush(card, int(sys.stdin.readline()))

while(len(card) != 1):
    x = heapq.heappop(card)
    y = heapq.heappop(card)
    heapq.heappush(card, x+y)
    result += (x+y)
print(result)

```

이 문제는 카드 묶음 리스트 중에서 가장 작은 두개의 카드 묶음을 꺼내야 한다.  
그래서 pop을 할 때 가장 작은 원소를 return하는 heap 큐 알고리즘을 사용했다.

### 참고 설명

https://python.flowdas.com/library/heapq.html